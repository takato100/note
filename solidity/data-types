データ型について

- storage
    - コントラクトとともに生き続ける

- stack
    - stackであり、一時的な置き場

- memory
    - stackよりも複雑　RAM的なもの？

- calldata
    - 関数の引数用のデータ領域
    - 変更不可
    - ex
    function msgData internal v v returns (bytes calldata) {}
    - コントラクトに対して送信されたデータをcalldataに格納してあげている
    - つまり、いちコントラクトよりも外に置かれて、他のコントラクトを呼び出した際にも共有して使えるのかな
